#import "./routing-rule.graphql"
#import "./routing-destination.graphql"

fragment RoutingRuleSet on RoutingRuleSet {
  id
  routingRules {
    ...RoutingRule
  }
  questionPage {
    id
    description
    guidance
    plaintextTitle: title(format: Plaintext)
  }
  else {
    ...RoutingDestinationQuestionPage
    ...RoutingDestinationSection
  }
}
