mutation UpdateQuestionPage(
  $id: Int!
  $title: String
  $description: String
  $guidance: String
) {
  updateQuestionPage(
    id: $id
    title: $title
    description: $description
    guidance: $guidance
  ) {
    id
    title
    description
    guidance
    type
    mandatory
  }
}
