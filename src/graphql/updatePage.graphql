mutation UpdateQuestionPage(
  $id: Int!
  $title: String
  $description: String
  $guidance: String
  $type: QuestionType
) {
  updateQuestionPage(
    id: $id
    title: $title
    description: $description
    guidance: $guidance
    type: $type
  ) {
    id
    title
  }
}
