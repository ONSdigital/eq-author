#import "./fragments/questionnaire.graphql"

query GetQuestionnaire($id: ID!) {
  questionnaire(id: $id) {
    ...Questionnaire
    sections {
      id
      title
      position
      pages {
        id
        title
        position
      }
    }
  }
}
