#import "./fragments/questionnaire.graphql"

query GetQuestionnaire($id: ID!) {
  questionnaire(id: $id) {
    ...Questionnaire
    sections {
      id
      title
      pages {
        id
        title
        position
      }
    }
  }
}
