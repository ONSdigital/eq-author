#import "graphql/fragments/answer.graphql"

query GetQuestionPage($id: ID!) {
  questionPage(id: $id) {
    id
    title
    description
    position
    guidance
    answers {
      ...Answer
    }
  }
}
