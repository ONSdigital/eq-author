import styled from "styled-components";

const MainCanvas = styled.div`
  max-width: 50em;
  margin-left: auto;
  margin-right: auto;
  margin-bottom: 1em;
  position: relative;
`;

export default MainCanvas;
