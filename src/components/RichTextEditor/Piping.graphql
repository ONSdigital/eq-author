query GetQuestionnaire_Piping($id: ID!) {
  questionnaire(id: $id) {
    id
    sections {
      id
      title
      pages {
        ... on QuestionPage {
          id
          title
          answers {
            id
            label
            type
            ... on CompositeAnswer {
              childAnswers {
                id
                label
                type
              }
            }
          }
        }
      }
    }
  }
}
